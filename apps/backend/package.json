{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend for ImageGenAI",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'FastAPI backend build completed'",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "lint": "black . && flake8 .",
    "test": "pytest",
    "clean": "rm -rf __pycache__ .pytest_cache .coverage"
  },
  "dependencies": {},
  "devDependencies": {}
}
